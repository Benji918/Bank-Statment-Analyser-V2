# Bank Analyser Backend

FastAPI backend for bank statement analysis.

## Setup

1. Create a virtual environment: `uv venv`
2. Install dependencies: `uv sync`
3. Run the application: `uvicorn app.main:app --reload`
